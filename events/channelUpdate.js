module.exports = (client, channel, oldChannel, newChannel) => {
    /*let cUpdate = client.channels.get('167715230082662401');
    if (cUpdate) {
        cUpdate.send('O canal '+oldChannel+' foi atualizado!').then(msg => {
        msg.delete(60000)
      });
    }*/
  };